# Database Configuration
DATABASE_URL=sqlite:///./loan_management.db
# For PostgreSQL: DATABASE_URL=postgresql://username:password@localhost/loan_management

# Redis Configuration (optional)
REDIS_URL=redis://localhost:6379

# Security Settings
SECRET_KEY=your-secret-key-here-change-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=60
REFRESH_TOKEN_EXPIRE_DAYS=30

# CORS Configuration
BACKEND_CORS_ORIGINS=["http://localhost:3000", "http://localhost:5173", "http://127.0.0.1:3000", "http://127.0.0.1:5173"]

# Default Admin User
DEFAULT_ADMIN_EMAIL=admin@kimloanans.com
DEFAULT_ADMIN_PASSWORD=KimLoans2024!

# Business Settings
DEFAULT_REGISTRATION_FEE=800.00
DEFAULT_LOAN_LIMIT_MULTIPLIER=4
DEFAULT_GRACE_PERIOD_MINUTES=60
DEFAULT_REORDER_POINT=5
DEFAULT_CRITICAL_POINT=2

# M-Pesa Integration (leave empty if not using)
MPESA_CONSUMER_KEY=
MPESA_CONSUMER_SECRET=
MPESA_SHORTCODE=

# SMS Gateway Settings (leave empty if not using)
SMS_API_KEY=
SMS_API_URL=

# Email Settings
SMTP_TLS=True
SMTP_PORT=587
SMTP_HOST=smtp.gmail.com
SMTP_USER=
SMTP_PASSWORD=

# Password Settings
PASSWORD_MIN_LENGTH=8

# API Settings
API_V1_STR=/api/v1
